/* comentario de multiplas linhas */

    // comentario de linha unica

    // comando de saida de dados

    /* sempre que trabalhar com textos devemos coloca-lo entre aspas SIMPLES ''
    DUPLAS "" ou crase´´ */

    console.log("ola JavaScript!");

    /* Variaveis e Constantes Espaços identificados na memoria para armazenar dados temporarimente */

    // variavel
    // obs.: antigamente, era usado "var"
    let ano = 2022;

    // Constante
    const aluno = prompt();


    console.log(ano)
    console.log(aluno)

    console.log(ano,aluno)

    /* operações matematicas 
    +(adição)
    -(subtração)
    /(divisão)
    *(multiplicação)
     */

        let preco = 1500

        let desconto = 250

        let precoFinal = preco - desconto

        console.log(preco, desconto, precoFinal)

        let produto = "TV Led"

        /*  A TV Led custava 1500 reais. */

        // 1 forma (tradicional) - CONCATENAÇÃO
        console.log('A '+produto+' custava '+preco+' reais.');

        // 2 froma moderna
        console.log(`A ${produto} custava ${preco} reais.`);

        
